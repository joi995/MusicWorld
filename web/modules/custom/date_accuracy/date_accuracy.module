<?php

/**
 * @file
 * date accuracy module file.
 */

/**
 * Implements hook_theme().
 */
function date_accuracy_theme($existing, $type, $theme, $path) {
  return [
    'date_accuracy' => [
      'variables' => ['date' => NULL, 'accuracy' => NULL],
    ],
  ];
}
